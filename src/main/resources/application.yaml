spring:
  application:
    name: online-store
  datasource:
    url: jdbc:mysql://localhost:3306/online-store?createDatabaseIfNotExist=true
    username: root
    password: tiger
  jpa:
    show-sql: true
